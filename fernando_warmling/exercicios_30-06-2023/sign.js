const numeroPositivo = 5;
const numeroNegativo = -3;
const numeroZero = 0;

console.log(Math.sign(numeroPositivo)); // Saída: 1
console.log(Math.sign(numeroNegativo)); // Saída: -1
console.log(Math.sign(numeroZero)); // Saída: 0